---
const { date } = Astro.props;
---

<time datetime={date} class="sm:mb-2">
  {
    new Date(date).toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
