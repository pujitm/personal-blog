---
import { cn } from "../lib/util";
const { href, class: className } = Astro.props;
---

<a
  class={cn(
    "flex group flex-col rounded-lg p-4 border border-green-900 dark:border-rose-300 hover:bg-stone-100 dark:hover:bg-gray-800 transition duration-300 ease-in-out shadow",
    className
  )}
  href={href}
>
  <slot />
</a>
