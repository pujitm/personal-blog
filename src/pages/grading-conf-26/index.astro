---
import GradingConferenceLayout from "../../layouts/GradingConferenceLayout.astro";
import { essays, posterUrl } from "../../lib/grading-conf-26";

const title = "The Grading Conference 2026";
const description =
  "Short essays on grading, education, and The Grading Conference 2026.";
---

<GradingConferenceLayout title={title} description={description}>
  <main class="page-shell">
    <header class="hero">
      <p class="kicker">The Grading Conference 2026</p>
      <h1>Twenty short essays on grading and education</h1>
      <p class="dek">
        I am writing a series of responses to the conference's presentations
        and posters, starting from the 66 abstracts. The series is inspired by
        <a href={posterUrl}>a poster</a> I am presenting about grades as
        communication; the essays are written without AI assistance.
      </p>
    </header>

    <section class="intro-grid" aria-label="Series notes">
      <div>
        <h2>Premise</h2>
        <p>
          Grading debates often circle around pedagogy, fairness, motivation,
          and measurement. I am more interested in the underlying design
          problem: what grades are trying to communicate, what the institution
          demands of them, and whether those demands can be rearranged.
        </p>
      </div>
      <div>
        <h2>Index</h2>
        <ol class="essay-index">
          {
            essays.map((essay) => (
              <li>
                <a href={`/grading-conf-26/${essay.slug}`}>
                  {essay.abstractTitle}
                </a>
                <span>{essay.author}</span>
              </li>
            ))
          }
        </ol>
      </div>
    </section>

    <section class="essays" aria-label="Essays">
      {
        essays.map((essay) => (
          <article class="essay-card">
            <p class="essay-number">#{essay.number}</p>
            <div>
              <p class="inspired-by">Inspired by</p>
              <h2>
                <a href={`/grading-conf-26/${essay.slug}`}>
                  {essay.abstractTitle}
                </a>
              </h2>
              <p class="byline">{essay.author}</p>
            </div>
          </article>
        ))
      }
    </section>
  </main>
</GradingConferenceLayout>
